git

최초
Remove-Item -Path .\webapp\.git -Recurse -Force

git add webapp -A
git commit --amend --no-edit   # 또는 새 커밋 생성
git branch -M main
git push -u origin main

필요시
git status
git add .
git commit -m "feat: integrate Neon DB and keyword management UI"

----------------------------------------------


